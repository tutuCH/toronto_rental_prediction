export interface Coordinates {
    latitude: number;
    longitude: number;
}

export interface PredictParam {
    bedroom: number,
    bathroom: number,
    den: number,
    lat: number,
    long: number,
}